name: iosApp
options:
  createIntermediateGroups: true
configs:
  Debug: debug
  Release: release
targets:
  iosApp:
    type: application
    platform: iOS
    deploymentTarget: "14.0"
    configFiles:
      Debug: Configuration/Config.xcconfig
      Release: Configuration/Config.xcconfig
    sources:
      - path: iosApp
    info:
      path: iosApp/Info.plist
      properties:
        BGTaskSchedulerPermittedIdentifiers:
          - io.github.v2compose.auto-check-in
        CFBundleDisplayName: $(APP_DISPLAY_NAME)
        NSPhotoLibraryAddUsageDescription: V2compose 需要将图片保存到系统相册。
        UIApplicationSupportsIndirectInputEvents: true
        UIBackgroundModes:
          - fetch
        UILaunchScreen: {}
    settings:
      base:
        SWIFT_VERSION: 5.0
        PRODUCT_NAME: $(APP_PRODUCT_NAME)
        PRODUCT_BUNDLE_IDENTIFIER: $(APP_BUNDLE_IDENTIFIER)
        INFOPLIST_FILE: iosApp/Info.plist
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: $(DEVELOPMENT_TEAM)
        ENABLE_USER_SCRIPT_SANDBOXING: NO
        "EXCLUDED_ARCHS[sdk=iphonesimulator*]": x86_64
        FRAMEWORK_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/../shared/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)
        OTHER_LDFLAGS:
          - $(inherited)
          - -framework
          - shared
    dependencies:
      - framework: ../shared/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)/shared.framework
        embed: false
        codeSign: false
    preBuildScripts:
      - name: Build shared framework
        basedOnDependencyAnalysis: false
        script: |
          cd "$SRCROOT/.."
          ./gradlew :shared:embedAndSignAppleFrameworkForXcode \
            -Pcompose.ios.resources.platform="${PLATFORM_NAME:-$SDK_NAME}" \
            -Pcompose.ios.resources.archs="${NATIVE_ARCH_ACTUAL:-${ARCHS%% *}}"
